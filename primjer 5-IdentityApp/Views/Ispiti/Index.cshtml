@model IEnumerable<primjer_5_IdentityApp.Models.Ispit>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<br />
<br />
<form type="GET">

    Unesi tekst za filtriranje:
    <input type ="search" name="search" value="@ViewBag.Search"/>
    <input type ="submit" value="Traži" />

</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.predmet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Datm)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.predmet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Datm)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
